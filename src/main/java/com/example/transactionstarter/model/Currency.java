package com.example.transactionstarter.model;

/**
 * Currencies this service accepts. No candidate-specific variant was assigned,
 * so this set is a documented assumption (see README).
 */
public enum Currency {
    INR,
    USD,
    EUR
}
